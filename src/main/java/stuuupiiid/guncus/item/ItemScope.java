package stuuupiiid.guncus.item;

import stuuupiiid.guncus.data.ScopePart;

public class ItemScope extends ItemMetadata {
	public ItemScope(String unlocalized, String iconName, ScopePart[] metadatas) {
		super(unlocalized, iconName, metadatas);
	}
}
